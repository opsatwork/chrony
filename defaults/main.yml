---
# defaults file for opsatwork.chrony

chrony_packages:
  - chrony
  
chrony_config_pool:
  - pool.ntp.org

chrony_config_stratumweight:
  - 0

chrony_config_driftfile:
  - /var/lib/chrony/drift

chrony_config_allow: []

chrony_config_logdir: '/var/log/chrony'
